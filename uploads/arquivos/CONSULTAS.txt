SELECT CLI_CODIGO, CLI_NOME 
FROM CLIENTES;
-- CONTAR AS TUPLAS
SELECT COUNT(*)
FROM CLIENTES;

SELECT *
FROM CLIENTES;

SELECT PRO_CODIGO, PRO_DESCRICAO, PRO_VALOR_UNIT
FROM PRODUTOS;

SELECT PRO_CODIGO ID, PRO_DESCRICAO AS DESCRIÇÃO, 
	PRO_VALOR_UNIT "VALOR UNITÁRIO", 
    cast(PRO_VALOR_UNIT*1.15 as decimal(5, 2)) AS REAJUSTE FROM PRODUTOS;

DESC PRODUTOS;

SELECT CLI_CODIGO CÓDIGO, CLI_NOME NOME, CLI_CPF CPF, CLI_DATA_NASCIMENTO DATA_NASCIMENTO, CLI_NOME_CONTATO
FROM CLIENTES
ORDER BY CLI_NOME ASC;

SELECT *
FROM CLIENTES
ORDER BY CLI_NOME DESC;

DESC ATENDENTES;

SELECT PRO_CODIGO, PRO_CAT_CODIGO, PRO_DESCRICAO
FROM PRODUTOS
WHERE PRO_CAT_CODIGO = 2
OR PRO_CAT_CODIGO = 3;

SELECT PRO_CODIGO, PRO_CAT_CODIGO, PRO_DESCRICAO
FROM PRODUTOS
WHERE PRO_CAT_CODIGO NOT IN (1, 3);

INSERT INTO ATENDENTES VALUES (ATE_CODIGO, ATE_NOME, ATE_ULTIMO_ACESSO, ATE_RAMAL, ATE_EMAIL, ATE_PERFIL, ATE_STATUS),
	(1, "MARIA LIMA", "2023-08-14", NULL, "MARIALIMA@GMAIL.COM", 1, NULL);
    
SELECT *
FROM ATENDENTES
ORDER BY ATE_NOME, ATE_EMAIL ASC;
    
SELECT COUNT(*)
FROM ATENDENTES;

SELECT *
FROM ATENDENTES;

-- BUSCA QUEM MORA NO MUNICIPIO 11
SELECT CLI_CODIGO, CLI_NOME, CLI_MUN_CODIGO
FROM CLIENTES
WHERE CLI_MUN_CODIGO = 11;

SELECT * FROM VENDAS;

SELECT VEN_CODIGO, VEN_FORMA_PAGAMENTO, VEN_VALOR_TOTAL
FROM VENDAS;

-- TRAS OS VALORES  ENTRE 50 E 100
SELECT VEN_CODIGO, VEN_FORMA_PAGAMENTO, VEN_VALOR_TOTAL
FROM VENDAS
WHERE VEN_VALOR_TOTAL BETWEEN 50 AND 100
ORDER BY VEN_VALOR_TOTAL;

SELECT CLI_NOME, CLI_ENEDERECO
FROM CLIENTES
WHERE CLI_NOME LIKE "L%";